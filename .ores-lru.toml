protocol = "ores.lru-config.v1"
namespace = "org-mcp-server-template.rs"
roles = ["server"]

[redis]
urlEnv = "REDIS_URL"
keyPrefix = "ores:lru:v1:org-mcp-server-template.rs"
pubsubChannel = "ores:lru:v1:org-mcp-server-template.rs:events"
reconcileIntervalMs = 180000
reconnectMinMs = 1000
reconnectMaxMs = 30000

[defaults]
capacity = 1024
syncMode = "read_only"
overflowMode = "reject_and_reconcile"
