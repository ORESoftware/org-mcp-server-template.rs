schema = 2
[tasks.bootstrap]
run = ["sh conformance/check.sh --structural-only"]
[tasks.contracts]
run = ["sh conformance/check.sh --full"]
[tasks.precommit]
run = ["sh .githooks/pre-commit"]
[tasks.prepush]
run = ["sh .githooks/pre-push"]
[tasks.test]
run = ["sh conformance/check.sh --full"]
