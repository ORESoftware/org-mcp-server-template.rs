schema_version: ores.compose.v1
project: org-mcp-server-template
allow_lazy_start: false

services:
  org-mcp-server-template:
    name: org-mcp-server-template
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/org-mcp-server-template.rs"]
